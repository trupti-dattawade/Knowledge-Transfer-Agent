# TODO - First page redesign (Overview)

- [ ] Update `app/static/index.html` overview section (data-view="overview") to redesign hero/status area into “Today’s focus” with clearer next action.
- [ ] Add/adjust CSS in `app/static/styles.css` for the new overview layout; keep existing IDs used by `app.js`.
- [x] Update `app/static/app.js` only if required for any new placeholders/IDs introduced in the overview redesign.

- [ ] Verify by running the app and checking that:
  - overview metrics render (total/completed/pending/awaiting)
  - workflow pulse still updates phases
  - no JS console errors

